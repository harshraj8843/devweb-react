
import { bgDarkBlue, heights } from '../../../../theme';

const styles = {
  root: {
    background: bgDarkBlue,
    position: 'sticky',
    top: 0,
    left: 0,
    width: '100%',
    height: heights.navBar,
  },
};

export default styles;